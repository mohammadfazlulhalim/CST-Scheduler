<div>
    <div id="modal">

    </div>
    <div id="main">
        <!--change this to a loop later-->
        <div id="tabBar">

        </div>
        {{#each groupArray as | grpLetter | }}
            <div id="group{{grpLetter}}">
                <div id="calender{{grpLetter}}">
                    <h2>Group {{grpLetter}} Calendar</h2>
                    <table class="table">
                        <thead>
                        <th scope="col">

                        </th>
                        <th scope="col">
                            Monday
                        </th>
                        <th scope="col">
                            Tuesday
                        </th>
                        <th scope="col">
                            Wednesday
                        </th>
                        <th scope="col">
                            Thursday
                        </th>
                        <th scope="col">
                            Friday
                        </th>
                        </thead>
                        <tr>

                        </tr>
                        {{#loop i = 0; i < 8; i++}}  <!--loops each timeslot, outer loop is hours, inner loop is days  -->
                            <tr>
                                <td>{{TIMES[i]}}</td>
                                {{#loop j = 1; j < 6; j++}}
                                    <td>groupArray[grpLetter].timeslotMatrix[j][i]</td>
                                {{/loop}}
                            </tr>
                        {{/loop}}
                    </table>

                </div>
                <div id="courseList{{grpLetter}}">
                    <h2>Group {{grpLetter}} Courses</h2>
                    {{#each COArray as | coObj | }}
                        {{#if eq coObj ../grpLetter }}

                            <div class="card">
                                <h3>Name: {{coObj.name}}</h3>
                            </div>
                        {{/if}}
                    {{/each}}

                </div>
            </div>
        {{/each}}

    </div>
</div>
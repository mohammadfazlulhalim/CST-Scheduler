const CourseOffering = require('../../../private/javascript/CourseOffering');

describe('group', () => {
  test(' with one letter is valid', () => {
  });

  test(' with one number is valid', () => {
  });

  test(' that is null is valid', () => {
  });

  test(' that is a lowercase letter is invalid', () => {
  });

  test(' with multiple characters is invalid', () => {
  });

  test(' with special characters is invalid', () => {
  });
});
